/**
 * 改变原数组
 * sort
 * reverse
 * splice
 * push
 * pop
 * unshift
 * shift
 */

/**
 * 不改变原数组
 * concat newArr
 * filter newArr
 * some boolean
 * every boolean
 * find 返回满足条件的元素 value
 * findIndex 返回满足条件的元素位置 index
 * forEach 无返回值 array.forEach(function(currentValue, index, arr), thisValue)
 * map 有返回值 newArr
 * slice array.slice(start, end) newArr
 * includes arr.includes(searchElement) boolean
 * indexOf index
 * lastIndexOf index
 * reduce 累加器
 * reduceRight array.reduceRight(function(total, currentValue, currentIndex, arr), initialValue)
 *
 * fill
 * keys
 * from
 * isArray boolean
 * toString
 * valueOf
 */
